import {entitiesPerPage, host} from "../../config/appConfig";

class ExplorationServiceImpl {
    private readonly host: string = host;
    private readonly entitiesPerPage: number = entitiesPerPage;
}